import AlertDialogPage from "./page.client";

export const metadata = { title: "Alert Dialog" };

const AlertDialog = () => {
  return (
    <div>
      <AlertDialogPage />
    </div>
  );
};

export default AlertDialog;
