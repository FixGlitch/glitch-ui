import ScrollAreaPage from "./page.client";

export const metadata = { title: "Scroll Area" };

const ScrollArea = () => {
  return (
    <div>
      <ScrollAreaPage />
    </div>
  );
};

export default ScrollArea;
