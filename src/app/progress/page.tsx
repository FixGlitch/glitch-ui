import ProgressPage from "./page.client";

export const metadata = { title: "Progress" };

const Progress = () => {
  return (
    <div>
      <ProgressPage />
    </div>
  );
};

export default Progress;
